server:
  port: 8081

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tarunvermaMI6/spring-app-config
          clone-on-start: true